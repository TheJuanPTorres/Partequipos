node_modules
.next
out
build
coverage
next-env.d.ts
package-lock.json
public
*.min.js

# Archivos generados por Payload (se reescriben con `generate:types`/`generate:importmap`)
src/payload-types.ts
src/app/(payload)/
# Migraciones generadas por `payload migrate:create`
src/migrations/

# Artefactos generados por `npm run crawl` (se reescriben en cada rastreo)
docs/crawl-reporte.md
docs/url-map.csv
docs/inventario-repuestos.csv

# Artefactos generados por `npm run redirects:map`
docs/redirects-cobertura.md
docs/redirects-map.csv

# Componentes COPIADOS del sistema de diseño del cliente con su CLI (§10.27).
# No se formatean a propósito: así el fichero del repositorio sigue comparable
# BYTE A BYTE con `https://ui.partequipos.com/r/<componente>.json`, que es la
# comprobación de cadena de suministro cuando se añade o se actualiza uno.
# Si algún día se edita uno de verdad, se saca de esta lista y se formatea.
src/components/ui/partequipos-wordmark.tsx
